<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /test/hil-v2/storage/

    RewriteCond %{REQUEST_FILENAME} -f [OR]
    RewriteCond %{REQUEST_FILENAME} -d
    RewriteRule ^ - [L]

    RewriteRule ^(.*)$ /test/hil-v2/public/storage/$1 [L]
</IfModule>
